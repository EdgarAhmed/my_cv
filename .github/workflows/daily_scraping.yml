name: Daily Scraping Job

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  run-scraping:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
        
    - name: Install Chrome
      run: |
        sudo apt-get update
        sudo apt-get install -y wget unzip gnupg
        
        # Instalar Chrome desde repositorio oficial de Google
        wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo gpg --dearmor -o /usr/share/keyrings/google-chrome-keyring.gpg
        echo "deb [arch=amd64 signed-by=/usr/share/keyrings/google-chrome-keyring.gpg] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
        sudo apt-get update
        sudo apt-get install -y google-chrome-stable
        
        # Verificar instalación
        google-chrome --version
        
    - name: Install Python dependencies
      run: |
        pip install --upgrade pip
        
        # Instalar dependencias principales del scraping
        pip install selenium pandas webdriver-manager lxml
        
        # Instalar pydrive para Google Drive
        pip install pydrive
        
        # Si tienes requirements.txt, también instálalo
        if [ -f "requirements.txt" ]; then
          pip install -r requirements.txt
        fi
        
    - name: Prepare Google Drive authentication (if needed)
      run: |
        echo "ℹ️  Para usar Google Drive en CI/CD, necesitarías:"
        echo "1. Configurar credenciales de servicio de Google"
        echo "2. Crear un archivo client_secrets.json"
        echo "3. Configurar un token de acceso"
        echo ""
        echo "Por ahora, la parte de Google Drive se omitirá automáticamente"
        
    - name: Run Python scraping script
      env:
        # Variables de entorno para Chrome en modo headless
        DISPLAY: :99
        PYTHONUNBUFFERED: 1
        # Variable para omitir Google Drive en CI/CD
        SKIP_GOOGLE_DRIVE: "true"
      run: |
        echo "📁 Directorio de trabajo: $(pwd)"
        echo "📁 Contenido del directorio scraping_web/:"
        ls -la scraping_web/
        
        echo "🚀 Iniciando scraping de ebooks de MediaMarkt..."
        
        # Crear directorio para resultados si no existe
        mkdir -p scraping_results
        
        # Ejecutar el script - capturar salida para debugging
        python scraping_web/scrip_ebooks_01.py 2>&1 || echo "Script terminó con errores"
        
        echo "✅ Script ejecutado. Verificando resultados..."
        
        # Listar archivos generados
        echo "📄 Archivos generados:"
        ls -la scraping_results/ 2>/dev/null || echo "No hay resultados en scraping_results/"
        ls -la *.csv 2>/dev/null || echo "No hay archivos CSV en el directorio principal"
        
    - name: Upload results
      uses: actions/upload-artifact@v4
      with:
        name: scraping-output
        path: |
          scraping_results/*.csv
          *.csv
        retention-days: 7
        
    - name: Notify on failure
      if: failure()
      run: |
        echo "❌ El scraping de MediaMarkt ha fallado"
        echo "Revisa los logs para más detalles"
        echo ""
        echo "Posibles causas:"
        echo "1. Problemas de conexión a MediaMarkt"
        echo "2. Cambios en la estructura HTML del sitio"
        echo "3. Timeout en la ejecución"
        echo "4. Falta de dependencias"
